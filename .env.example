# Copy this file to .env.local for local dev.
# Both variables must also be set in Vercel (Production + Preview + Development).
# Only "VITE_*" variables are exposed to the browser bundle.

VITE_SUPABASE_URL=https://csaequazndafhgotnlif.supabase.co
VITE_SUPABASE_ANON_KEY=
